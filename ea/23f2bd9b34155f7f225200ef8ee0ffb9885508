Task: Phase 1 evidence-integrity validator. Changes: added manifest validator and positive/negative tests for durable paths, hashes, genuine revisions, distinct roles, and unit membership. Verification: uv run ruff check; uv run ruff format; uv run pytest -q tests/test_validate_au_calibration_evidence.py (3 passed). Remaining risks: current calibration artifacts remain ephemeral and require restricted intake; no maturity claims.
