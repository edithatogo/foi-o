# Claim ledger

Every material paper claim must link to immutable source/data/software/profile,
the derivability status, exact version lock and reviewer disposition. Candidate
or legally indeterminate claims remain explicitly qualified. This directory is
the canonical location for the machine-readable claim ledger; generated reports
belong under `reports/paper/<release>/<run_id>/`.
