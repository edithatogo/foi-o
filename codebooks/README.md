# Empirical codebook overlays

These files govern candidate definitions; they do not replace FOI-O annotation
or gold-set records. Assertions always identify the codebook version used.
Stable-label changes require a label-change request and human approval.
