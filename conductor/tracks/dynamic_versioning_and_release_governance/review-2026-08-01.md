# Track review — dynamic versioning and release governance

Review date: 2026-08-01

Current repository-owned checks pass:

```text
uv run python scripts/version_tool.py check
registry and lock are 1.0.0
uv run python scripts/check_release_metadata.py
release metadata consistent: 0.8.1
uv run python tests/validate_repo.py
repository validation ok
```

The multiaxial version registry, compatibility lock, and release checker remain
fail-closed. No release, publication, profile promotion, registry submission,
or external mutation was performed. The track remains open only at its declared
human gates.
