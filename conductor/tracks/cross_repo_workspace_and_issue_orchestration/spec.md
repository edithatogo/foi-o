# cross repo workspace and issue orchestration

This track implements the live FOI-O global programme slice for **FOIO-REQ-SELF-001**. It preserves existing NZ evidence, uses immutable cross-repository references, and fails closed at human gates.
