# Track review — global context runtime hardening

Review date: 2026-08-01

Repository-owned drift found and repaired during this review:

- Refreshed the ontology coverage matrix and generated maturation summary to
  the live inventory (203 JSON Schema files and 186 Python test modules).
- Added a narrow decoded-record type assertion in the bounded Australian
  annotation validator and satisfied the repository's TC006 lint rule.

Validation at commit `c1464ee`:

| Check | Result |
| --- | --- |
| `uv run pytest -q` | Passed: 1233 passed, 4 skipped. |
| `uv run ruff check src tests scripts` | Passed. |
| `uv run ruff format --check src tests scripts` | Passed: 383 files already formatted. |
| `uv run basedpyright` | Passed: 0 errors, 0 warnings, 0 notes. |
| `uv run ty check src tests scripts` | Passed. |
| `uv run python tests/validate_repo.py` | Passed: repository validation ok. |
| `uv run python scripts/validate_requirements.py` | Passed: 11 requirements, no errors. |
| `uv run python scripts/validate_workflows.py` | Passed. |
| `uv run python scripts/version_tool.py check` | Passed: registry and lock are 1.0.0. |
| `uv run python -m foi_o_nz.cli validate-capability-registry` | Passed: 7 capabilities, no duplicates. |
| `uv run python scripts/check_release_metadata.py` | Passed: release metadata consistent at 0.8.1. |
| `git diff --check` | Passed. |

The track remains `in_progress` because publication, legal/profile promotion,
registry submission, and external mutation remain explicit human gates. No
release, publication, profile promotion, registry submission, or external
mutation was performed.
