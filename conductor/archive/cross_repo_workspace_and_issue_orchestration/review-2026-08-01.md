# Track review — cross-repository workspace and issue orchestration

Review date: 2026-08-01

The read-only workspace doctor was rerun from the authoritative FOI-O checkout.
It found duplicate remote identities that must remain preserved and must not be
deleted automatically:

- FOI-O: `/Volumes/PortableSSD/GitHub/foi-o` and
  `/Volumes/PortableSSD/GitHub/foi-o-wiring`.
- fyi-archive: `/Volumes/PortableSSD/GitHub/fyi-archive` and
  `/Volumes/PortableSSD/GitHub/fyi-archive-au-rtk-replay-30236042144`.

The doctor is read-only and performed no fetch, pull, clone, edit, or delete.
The duplicate findings are an operational review result, not an authorization
to remove or merge workspaces. The committed `workspace-audit.md` and
`workspace.lock.yaml` remain historical snapshots and are not silently
rewritten from this observation.

The external-mutation, publication, and legal/profile-promotion gates remain
open. No external repository action was performed.

## Current validation

- Workspace doctor: 10 repository identities inspected; duplicate remote
  identities remain for FOI-O (2) and fyi-archive (2).
- Focused control tests: 13 passed.
- Requirements, workflow, repository, Ruff, formatting, and diff checks passed.

The duplicate checkouts remain preserved. They are not an authorization to
delete, reset, merge, fetch, pull, push, or otherwise mutate any workspace.

## Review finding

- **Medium — fixed:** `output-ledger.yaml` recorded an absolute `/tmp` runtime
  path. This was not a reproducible repository-relative artifact reference and
  conflicted with the bounded-path contract. The runtime observation remains
  described here, without claiming it is a committed output.

## Closeout recheck

The read-only workspace doctor was rerun before closeout. It still reports
duplicate remote identities for FOI-O at `/Volumes/PortableSSD/GitHub/foi-o`
and `/Volumes/PortableSSD/GitHub/foi-o-wiring`, and for fyi-archive at
`/Volumes/PortableSSD/GitHub/fyi-archive` and
`/Volumes/PortableSSD/GitHub/fyi-archive-au-rtk-replay-30236042144`. The main
fyi-archive checkout is dirty. No checkout was fetched, reset, deleted, merged,
or otherwise changed.

The full local validation set passed again: requirements, workflow and
repository validators; 13 focused tests; Ruff lint and formatting; and diff
checks. The track is not archive-eligible because its closeout contract still
requires the remaining non-machine human gates.

## Hosted verification

Read-only GitHub verification confirmed that `rac-conformance#146` is open,
in progress, and linked to project 29, `FOI-O Global Next-Generation
Programme`. Project 10 is a separate project and no reconciliation mutation
was required. The exact observation is recorded in
`hosted-verification-2026-08-01.json`.

This resolves the issue/project verification sub-gate. The approved
dispositions classify external mutation as no action required, and publication,
release, submission, profile promotion and legal certification as not
applicable to this coordination-only track.
