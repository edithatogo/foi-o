# Ontology registry readiness

The machine-readable ledger is authoritative for status. Every registry is
`not_verified` unless an immutable submission/response record exists. This
repository prepares the global namespace, version IRI, documentation,
SHACL-quality checks and migration aliases; it does not submit autonomously.
