# Proposed v0.8 to v1.0 migration note

V1.0 is intended as a release-quality baseline rather than a semantic rewrite.
Preserve the package name, namespace, event identifiers, source labels, and
existing records. Record schema versions and release manifests. Any discovered
breaking change must be listed with real before/after fixtures before tagging.
